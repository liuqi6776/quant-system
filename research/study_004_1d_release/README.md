# Study 004 — T+1 新闻舆情策略（1日持仓 + 高开过滤）

## 策略概述

本研究实现了一套基于新闻舆情的 A 股 T+1 量化策略，具备以下核心特征：

- **模型目标**：预测 `(T+2 收盘价 - T+1 开盘价) / T+1 开盘价`，即"T+1 开盘买入、T+2 收盘卖出"的单日收益
- **入场时机**：T 日收盘后模型预测概率 ≥ 阈值，T+1 集合竞价结束后（9:25）按开盘价买入
- **高开过滤器**：T+1 开盘价相对 T 日收盘价涨幅在 **2%～6%** 之间（排除没有反应的股票，也排除开盘直接涨停的陷阱）
- **持仓周期**：T+1 开盘买入，T+2 收盘卖出（2 日持仓）
- **严格 T+1 约束**：当日不能卖出当日买入的股票

## 代码结构

```
study_004_1d_release/
├── README.md                        ← 本文件
├── scripts/
│   ├── step1_build_features.py      ← 特征构建（无 shift(-2) 残留）
│   ├── fix_1d_target.py             ← 将 target 修正为 T+1 开盘买入基准
│   ├── train_1d_open.py             ← Walk-forward 月度滚动训练
│   ├── config.py                    ← 数据路径配置
│   └── backtest_1d_gap_filter.py    ← 主回测脚本（含高开过滤 + 网格搜索）
└── results/
    ├── 1d_strategy_limit_filter.png          ← 权益曲线图
    └── 1d_strategy_limit_filter_results.json ← 完整网格结果
```

## 数据管道说明

数据和预测文件均存放于 `studies/study_004_systematic/`：

| 文件 | 说明 |
|------|------|
| `data/all_features_v2.parquet` | 最新特征文件（已消除 shift(-2) 残留） |
| `predictions/predictions_1d_open_wf_monthly.parquet` | 月度滚动 WFO 预测（T+1 开盘 target） |

## 回测逻辑

### 买入条件（T+1 日）
1. `prob ≥ threshold`（模型置信度过滤）
2. T 日未涨停（涨停当日无法正常建仓）
3. T+1 开盘未涨停（无法以合理价格买入）
4. **高开幅度在 2%～6% 之间**：`0.02 < (T+1_open - T_close) / T_close < 0.06`

### 卖出条件（T+2 日）
- 正常持有至 T+2 收盘卖出
- 止损触发（可配置，建议 -5%）：T+2 日内止损，以止损价卖出
- 止盈触发（可配置，建议 +5%）：T+2 日开盘或盘中达到止盈价时卖出
- 跌停时延迟卖出（避免无法成交）

### 仓位管理
- `pos_size = 1 / (hold_days * max_pos)`
- 以 `max_pos` 控制同期最大持仓数量
- 交易费用：买 0.1% + 卖 0.1%（合计 0.2%/次）

## 最终回测结果（2022-2026，Clean 数据）

> **数据说明**：基于 step1_build_features.py 重新生成的特征（无 shift(-2) 残留）+ fix_1d_target.py 修正目标 + predictions_1d_open_wf_monthly.parquet（月度 WFO 预测）

### BEST COMBO per threshold（按测试集 Sharpe 排序）

| 阈值 | 持仓数 | 止损 | 止盈 | 测试集 Sharpe | 测试集 CAGR | 测试集胜率 |
|------|--------|------|------|--------------|------------|----------|
| 0.50 | 3 | -5% | +5% | **4.63** | 65.2% | 37.2% |
| 0.52 | 3 | -5% | +5% | 4.49 | 62.6% | 36.5% |
| 0.54 | 3 | -5% | +5% | 4.19 | 55.7% | 34.7% |
| 0.56 | 10 | -5% | +5% | 4.03 | 18.4% | 37.2% |
| 0.58 | 10 | -10% | +5% | 4.01 | 17.1% | 33.0% |
| 0.60 | 10 | -10% | +5% | 3.84 | 14.2% | 27.4% |
| 0.62 | 10 | -10% | +5% | 2.80 | 8.4% | 21.8% |
| 0.64 | 3 | -10% | +5% | 2.44 | 21.1% | 17.2% |
| 0.66 | 3 | -5% | +5% | 2.21 | 16.7% | 14.0% |

> **全局最优**：`threshold=0.50, max_pos=3, stop_loss=-5%, take_profit=+5%`
> Test Sharpe = **4.63**，Test CAGR = **65.2%**，Max DD ≈ -6%

### 关键结论

1. **止盈 5% 是关键**：加入 5% 止盈后，Sharpe 从 4.15（无止盈）提升到 **4.63**。高开动量股在当日达到 5% 涨幅后往往回调，及时止盈能显著改善收益质量。

2. **高开 2%～6% 过滤器效果极显著**：相比无过滤版本，胜率提升了约 10-15 个百分点，Sharpe 从 2.x 提升到 4.x 量级，是策略成立的核心条件。

3. **低阈值 + 集中仓位（pos=3）是最优组合**：满足高开条件的票已经是高质量筛选集，不需要过度提高 prob 阈值。直接用 `th=0.50, pos=3` 充分利用模型召回率。

4. **无未来函数，时间对齐严格验证**：
   - 特征构建时已消除 `shift(-2)` 残留
   - target = `(T+2 close - T+1 open) / T+1 open`（由 fix_1d_target.py 生成）
   - 回测以 `T+1 open` 为 `bp`（买入价）
   - 高开计算使用 `T+1 open` vs `T 日 close`（非 T+1 日 close）

## 实盘操作指引

1. **T 日盘后**：拉取最新数据 → 运行预测 → 筛选 `prob ≥ 0.50` 的标的（最多 3 只）
2. **T+1 集合竞价结束（9:25）**：计算每只候选标的的集合竞价价格相对昨收涨幅
   - 涨幅在 2%～6% 之间 → **买入**（以开盘价委托）
   - 涨幅不在此区间 → 跳过
3. **T+2 全天监控**：
   - 日内涨幅达 +5% → 止盈卖出
   - 日内跌幅达 -5% → 止损卖出
   - 若未触发，**收盘前卖出**
